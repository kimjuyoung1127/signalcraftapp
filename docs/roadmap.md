# SignalCraft Mobile Roadmap

## 🗺️ 통합 개발 로드맵 (Backend & Frontend)
나중에 뜯어고치는 일을 막기 위해, **"백엔드는 API화", "프론트엔드는 모듈화"**에 집중하는 로드맵입니다.

## 📅 진행 상황 (2025-11-25 기준)

### ✅ 완료된 작업
- **[✓] 기본 네비게이션 구조**: AuthStack + MainTab 완료
- **[✓] Industrial Cyberpunk 디자인 시스템**: 다크 테마 및 네온 컬러 구현
- **[✓] 오디오 비주얼라이저**: Reanimated 기반 원형 파동 애니메이션 (60fps)
- **[✓] 장비 상태 관리**: Zustand Store 기반 상태 저장 및 갱신
- **[✓] 데모 컨트롤**: 테스트용 상태 강제 변경 기능 (심사위원용)
- **[✓] 설정 화면**: 전체 설정 페이지 및 톱니바퀴 버튼 내비게이션
- [✓] 동적 상태 대시보드: 오디오 전용 지표 및 고조파 분석 차트
- [✓] 실시간 데이터 연동: 상태별 동적 지표 변화 구현
- [✓] 스크롤 가능한 레이아웃: 새로운 콘텐츠 추가에 따른 UI 최적화
- [✓] 한글화 적용: 전체 UI 한국어 번역 완료
- [✓] 백엔드 연동 기초: JWT 로그인, 회원가입, 자동 로그인, 토큰 관리

### 🔄 현재 진행 중
- [!] Phase C: 오디오 분석 파이프라인 구축: 녹음 -> 업로드 -> 분석 결과 확인

### 🔌 Backend Roadmap (FastAPI API Expansion)

#### ✅ 완료된 백엔드 준비
- [✓] Mock Service 구조: 데모 모드를 위한 가상 데이터 서비스
- [✓] API Layer 설계: 백엔드 연동을 위한 서비스 레이어 준비
- [✓] Phase 0: Docker Compose 인프라 구축 (필수)
- [✓] Phase 1: 보안 및 인증 (FastAPI 기반)

- [ ] Phase 2: 데이터 조회 API 최적화 (FastAPI)
    - [ ] `GET /api/mobile/devices`: 모바일 대시보드용 경량화된 장비 리스트 반환 (상태, 모델명만).
    - [ ] `GET /api/mobile/audio-stats/{device_id}`: 오디오 분석용 최근 데이터 반환
    - [ ] `GET /api/mobile/realtime/{device_id}`: 실시간 장비 상태 스트리밍 (Polling 기반으로 시작, 필요 시 WebSocket으로 확장)

- [✓] **Phase 3: AI 분석 파이프라인 연동 (FastAPI + Celery)**
    - [✓] `POST /api/mobile/upload`: multipart/form-data로 모바일 녹음 파일 수신 로직.
    - [✓] Celery 연동: 업로드 즉시 Task ID 반환 (Non-blocking).
    - [✓] `GET /api/mobile/result/{task_id}`: 폴링용 상태 확인 API.
    - [ ] `GET /api/mobile/harmonics/{device_id}`: 고조파 분석 데이터 API
    - [ ] 오디오 분석 모델 통합: SignalCraft AI 모델을 Celery 작업으로 구현

    ##### **[신규] Phase 3.1: Feature-Based Backend 오디오 분석 모듈 구현**
    *   **목표**: `app/features/audio_analysis/` 폴더 내에 모든 오디오 분석 관련 백엔드 로직을 모아 구현합니다.
    *   **Step 1: Feature 폴더 및 파일 생성**
        - [✓] `app/features/audio_analysis/` 디렉토리 생성.
        - [✓] `app/features/audio_analysis/__init__.py` 생성.
        - [✓] `app/features/audio_analysis/models.py` 파일 생성.
        - [✓] `app/features/audio_analysis/schemas.py` 파일 생성.
        - [✓] `app/features/audio_analysis/router.py` 파일 생성.
        - [✓] `app/features/audio_analysis/service.py` 파일 생성.
    *   **Step 2: DB 모델 구현 (`app/features/audio_analysis/models.py`)**
        - [✓] `AudioFile` (업로드된 오디오 파일 정보) SQLAlchemy 모델 추가.
        - [✓] `AIAnalysisResult` (오디오 분석 결과) SQLAlchemy 모델 추가.
    *   **Step 3: Pydantic 스키마 구현 (`app/features/audio_analysis/schemas.py`)**
        - [✓] `AudioFile` 및 `AIAnalysisResult` 응답/요청을 위한 Pydantic 스키마 정의.
        - [✓] `UploadResponse`, `AnalysisResultResponse` 등 API 응답 형식 정의.
    *   **Step 4: Celery Task 구현 (`app/worker.py`)**
        - [✓] `analyze_audio_task` 함수 추가.
        - [✓] 파일 경로와 `analysis_result_id`를 받아 분석 상태 및 결과 업데이트 로직 구현.
    *   **Step 5: 서비스 로직 구현 (`app/features/audio_analysis/service.py`)**
        - [✓] `save_audio_file`: 오디오 파일을 서버에 저장하고 `AudioFile` DB 레코드를 생성하는 함수.
        - [✓] `create_analysis_result`: 초기 `AIAnalysisResult` 레코드 (PENDING 상태)를 생성하는 함수.
        - [✓] `get_analysis_result_by_id`: `AIAnalysisResult`를 ID로 조회하는 함수.
        - [✓] `update_analysis_result`: `AIAnalysisResult` 상태 및 결과 데이터를 업데이트하는 함수.
    *   **Step 6: API 라우터 구현 (`app/features/audio_analysis/router.py`)**
        - [✓] `APIRouter` 정의.
        - [✓] `POST /upload` 엔드포인트 구현 (파일 수신, 저장, DB 기록, Celery 호출).
        - [✓] `GET /result/{task_id}` 엔드포인트 구현 (분석 결과 조회).
    *   **Step 7: FastAPI `main.py`에 라우터 등록 (`main.py`)**
        - [✓] `from app.features.audio_analysis import router as audio_analysis_router` 추가.
        - [✓] `app.include_router(audio_analysis_router.router, prefix="/api/mobile", tags=["Mobile"])` 추가.

### 📱 Frontend Roadmap (React Native App)

#### ✅ 완료된 프론트엔드 기능
- [✓] Phase 1: 뼈대 및 디자인 시스템 (MVP 핵심)
- [✓] Phase 2: 대시보드 & 시각화
- [✓] Phase 2.5: 백엔드 API 연동 (Auth & Devices)

#### ✅ Phase C: 오디오 분석 파이프라인 (Audio Pipeline) - 구현 완료

**목표**: 실제 기계 소음을 녹음하여 AI 서버(또는 Mock)로 전송하고, 진단 결과를 받아 사용자에게 시각적으로 보여줍니다.

*   **Step 1: 환경 설정 및 UI 구조 (Setup & Navigation)**
    - [✓] 패키지 설치: `expo-file-system` 설치 (안정적인 파일 업로드용).
    - [✓] 권한 설정: `app.json`에 `expo-av` 플러그인 및 권한 문구(`NSMicrophoneUsageDescription`) 추가.
    - [✓] AnalysisScreen 생성: 녹음 및 분석 전용 화면 뼈대 작성.
    - [✓] 네비게이션 개편: `MainTabNavigator` 수정 (`Monitor` | `Analysis` | `Settings`).
        - [✓] 가운데 탭 강조: 아이콘 크기 확대 및 위치 조정을 통해 진단 도구 느낌 강화.

*   **Step 2: 녹음 기능 구현 (Recording)**
    - [✓] Audio Recorder Hook: `useAudioRecorder` 커스텀 훅 작성 (`expo-av`).
    - [✓] 녹음 UI 구현: 녹음 버튼, 시간 표시, 파형 애니메이션 연동.
    - [✓] 권한 요청 로직: 최초 접근 시 마이크 권한 요청 및 거부 처리.
    - [✓] API 호환성 패치: `expo-av` 및 `expo-file-system` 최신 버전 API 호환성 문제 해결.

*   **Step 3: 분석 서비스 및 결과 (Analysis Logic)**
    - [✓] Upload Service: `FileSystem.uploadAsync` 사용하여 파일 전송 (JWT 헤더 수동 주입).
    - [✓] Mock Analysis: 데모 모드일 때 가짜 분석 결과(정상/위험 랜덤) 반환.
    - [✓] 결과 카드 UI: 분석 상태(대기/분석중/완료)에 따른 UI 변화.
    - [✓] Polling Logic: 분석 요청 후 결과가 나올 때까지 상태 주기적 확인.

#### 🎨 UI/UX 향상사항
- [ ] Micro-interactions: 부드러운 터치 피드백 및 트랜지션
- [ ] 네트워크 상태 표시: 연결/연결 끊김 상태 UI 피드백
- [ ] 애니메이션 최적화: 120fps 지원 및 저사양 기기 대응
- [ ] Dark Mode 응답성: 시스템 설정 기반 자동 전환
- [ ] 접근성: VoiceOver 및 TalkBack 지원

---

**마지막 업데이트**: 2025-11-25
**다음 업데이트 예정**: Phase C 구현 완료 후
**담당자**: SignalCraft Mobile Development Team
